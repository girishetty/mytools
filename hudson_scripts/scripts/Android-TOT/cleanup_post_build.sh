#!/bin/sh
#$1 - where the build o/p is stored

#We haven't build anything in here locally, so don't clean up anything
#Leave the generated images as it is for future reference.
#if we want to clean up anything, add here

rm -rf $1
