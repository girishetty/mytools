#include <stdio.h> 
#include <math.h>
#include <time.h>

double l;
void showIndia ();
int showMoon (int _, int o, int O);

int main(int _, int o, int O) { 

	showIndia ();
	getchar();
	showMoon (_, o, O);

	return 1;
}

void showIndia () {
	int a,b,c; 
	int count = 1; 
	for (b=c=10;a="- FIGURE?, UMKC,XYZHello Folks,TFy!QJu ROo TNn(ROo)SLq SLq ULo+UHs UJq TNn*RPn/QPbEWS_JSWQAIJO^NBELPeHBFHT}TnALVlBLOFAkHFOuFETpHCStHAUFAgcEAelclcn^r^r\\tZvYxXyT|S~Pn SPm SOn TNn ULo0ULo#ULo-WHq!WFs XDt!" [b+++21] ;) 
	for(; a-- > 64 ; ) 
	putchar ( ++c=='Z' ? c = c/ 9:33^b&1); 
}


int showMoon (int _, int o, int O) {
	return putchar (
	(_--+22&&_+44&&showMoon(_,-43,_),_&&o)?(showMoon(-43,++o,O),((l=(o+21)/sqrt(3-
	O*22-O*O),l*l<4&&(fabs(((time(0)-607728)%2551443)/405859.-4.7+acos(l/2
	))<1.57))[" *"])):10 );
}
 